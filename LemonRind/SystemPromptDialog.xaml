<Window x:Class="SystemPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System prompt" Width="600" Height="500" ResizeMode="NoResize"
        Icon="Assets/AppIcon.ico"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/DialogButtonStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="DescriptionTextBlock" Foreground="Gray" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,10" />

        <!-- Selectable, read-only - same reasoning as this app's own reply/
             thinking text and the model details dialog's own llamacpp-args
             box: this is exactly the kind of text a user would want to
             copy out. -->
        <TextBox Grid.Row="1" x:Name="PromptTextBox" IsReadOnly="True" IsUndoEnabled="False"
                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12" Background="#F5F5F5" BorderThickness="0"
                 Padding="10" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="CloseButtonElement" Padding="14,7"
                    Style="{StaticResource DialogPrimaryButtonStyle}"
                    Click="OnCloseClick" IsDefault="True" IsCancel="True" Content="Close" />
        </StackPanel>
    </Grid>
</Window>
